{
  "name": "company-intelligence-engine",
  "version": "0.1.2",
  "description": "Company Intelligence Engine — structured dossier generation for downstream AI",
  "type": "module",
  "scripts": {
    "validate": "tsx src/validate.ts",
    "generate-empty": "tsx src/utils/empty-dossier.ts",
    "test": "vitest run",
    "eval": "tsx src/report/evals/runner/run-fixture.ts",
    "analyse-batch": "tsx src/report/runner/batch-analyse.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "playwright": "^1.58.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
